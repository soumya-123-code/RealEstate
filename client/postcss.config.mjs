/**
 * PostCSS config.
 *
 * This project is currently missing Tailwind/PostCSS plugins in package.json,
 * so keep this config minimal to avoid dev-server startup failures.
 */
const config = {
  plugins: [],
};

export default config;



