Options -MultiViews
RewriteEngine On

# Keep real files and directories untouched.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router SPA fallback.
RewriteRule ^ index.html [L]
